[class*="icon-"] {
    background-image: url({{spritesheet.escaped_image}});
}
{{#sprites}}
.icon-{{name}} {
    background-position: {{px.offset_x}} {{px.offset_y}};
    width: {{px.width}};
    height: {{px.height}};
}
@{{name}}-position: {{px.offset_x}} {{px.offset_y}};
{{/sprites}}